services:
  docs:
    image: weightmelters_local_docs
    container_name: weightmelters_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./weightmelters:/app/weightmelters:z
    ports:
      - '9000:9000'
    command: /start-docs
